fn fibbonacci_sequence(iterations: int, curr: int, prev: int): int {
    if (iterations == 0) {
        ret 0
    }
    print(curr)
    ret fibbonacci_sequence(iterations - 1, prev + curr, curr)
}

fn_ main() {
    let x: int = 20
    fibbonacci_sequence(x, 0, 1)
    rev
}